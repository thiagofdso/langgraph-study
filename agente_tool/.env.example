# agente_tool – variáveis de ambiente
#
# Copie este arquivo para `.env` na raiz de `agente_tool/` e preencha com
# valores reais antes de executar o agente. Nunca comite credenciais válidas.

GEMINI_API_KEY="coloque_sua_chave_do_gemini_aqui"

# Opcional: identificador padrão usado para execuções locais
DEFAULT_THREAD_ID="calculator-cli"
