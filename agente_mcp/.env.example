# Copied from agente_simples/.env to keep consistent defaults
GEMINI_API_KEY="replace-with-your-gemini-key"
GOOGLE_API_KEY="replace-with-your-google-key"

# Optional overrides specific to agente_mcp
MCP_DEFAULT_QUESTIONS='["Qual é o resultado de 2+2?", "Qual a previsão do tempo para hoje?"]'
MCP_AUTO_START_SERVERS="true"
MCP_LOG_LEVEL="INFO"
