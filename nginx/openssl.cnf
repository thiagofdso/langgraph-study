[req]
default_bits       = 2048
prompt             = no
default_md         = sha256
distinguished_name = dn
req_extensions     = req_ext

[dn]
CN = langgraph.local

[req_ext]
subjectAltName = DNS:langgraph.local